services:
  # Django Backend
  - type: web
    name: agridetector-backend
    env: python
    region: oregon
    buildCommand: |
      cd farmer_weather
      pip install --upgrade pip
      pip install -r requirements.txt
      python manage.py collectstatic --no-input
      python manage.py migrate
    startCommand: cd farmer_weather && gunicorn farmer_weather.wsgi:application --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.7
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: False
      - key: ALLOWED_HOSTS
        value: .onrender.com
      - key: DATABASE_URL
        fromDatabase:
          name: agridetector-db
          property: connectionString
    
  # PostgreSQL Database (optional - you can use SQLite for now)
  # - type: pgsql
  #   name: agridetector-db
  #   region: oregon
  #   plan: free
  #   databaseName: agridetector
  #   databaseUser: agridetector
